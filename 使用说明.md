# 博客使用说明

## 一、启动本地网站

在项目根目录运行：

```bash
cd "/Users/junchishen/Documents/Personal Blog"
python3 -m http.server 8765
```

浏览器打开：<http://localhost:8765/blog.html>

不要直接双击 `blog.html`，否则作者口令验证可能无法使用。

## 二、作者登录和 GitHub Token 的区别

这两个不是同一个东西：

| 名称 | 用途 | 在哪里获得 |
|---|---|---|
| 作者口令 | 打开博客的作者编辑功能 | 本机 `.env` 文件中的 `BLOG_OWNER_PASSPHRASE` |
| GitHub Token | 让博客读写 GitHub 仓库、发布文章 | GitHub 的 Fine-grained personal access token |

正确顺序是：先用作者口令登录，再连接 GitHub 并输入 Token。

### 作者口令

在博客页面底部点击“作者登录”，输入本机 `.env` 中的作者口令。

查看口令：

```bash
cd "/Users/junchishen/Documents/Personal Blog"
cat .env
```

只查看 `BLOG_OWNER_PASSPHRASE=` 后面的内容。`.env` 是私密文件，不要提交到 GitHub，也不要发给别人。

### GitHub Token

作者登录成功后，点击页面中的“连接 GitHub”。粘贴 GitHub 生成的 Token，点击“验证并保存”。

生成 Fine-grained token 时使用以下设置：

- Resource owner：`Junchi-Shen`
- Repository access：Only select repositories
- 选择：`Junchi-Shen/Junchi-Shen.github.io` 和 `Junchi-Shen/blog-private`
- Repository permissions：`Contents` 设置为 `Read and write`
- `Metadata: Read-only` 保持默认即可

Token 完整内容只会显示一次。忘记后不能查看原 Token，只能重新生成。不要把 Token 发给任何人。

## 三、写作和发布

1. 用作者口令登录。
2. 点击“写文章”。
3. 新文章默认是私密文章。
4. 确认文章可公开后，再将可见性改为“公开”。
5. 点击“连接 GitHub”，输入 Token 并验证保存。
6. 点击“发布到 GitHub”。

公开文章会写入：

- `Junchi-Shen/Junchi-Shen.github.io` 的 `content/posts.json`

私密文章会写入：

- `Junchi-Shen/blog-private` 的 `posts.json`

私密仓库必须保持 Private。公开文章任何人都能看到，发布后不要再放入密码、Token 或其他隐私内容。

## 四、常见问题

### 提示 `Wrong passphrase`

说明输入框是“作者登录”口令框。这里要输入 `.env` 中的 `BLOG_OWNER_PASSPHRASE`，不能输入 GitHub Token。

### 提示 `Not found, or token has no access to this repo`

说明 Token 没有授权两个仓库，或者仓库名称选错。重新检查 Repository access 和 `Contents: Read and write` 权限。

### Token 验证成功，但发布失败

检查 Token 是否过期，以及 `blog-private` 是否仍然是私有仓库。必要时在 GitHub 重新生成 Token，并在博客中重新验证保存。

### 本地修改没有发布

文章编辑会先保存在当前浏览器的 localStorage 中。只有点击“发布到 GitHub”后，其他设备和线上页面才能看到已发布内容。

## 五、修改作者口令

在项目根目录运行：

```bash
node tools/set-passphrase.mjs "新的口令至少十二位"
```

然后提交并推送生成后的 `assets/auth.js`：

```bash
git add assets/auth.js
git commit -m "rotate owner passphrase"
git push
```

`.env` 不要提交。
